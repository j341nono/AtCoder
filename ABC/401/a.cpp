#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main(){
    int s;
    cin>>s;
    if(s>=200&&s<=299)cout<<"Success"<<endl;
    else cout<<"Failure"<<endl;
}